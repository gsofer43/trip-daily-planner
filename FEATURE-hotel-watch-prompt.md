# משימה: מעקב זמינות מלונות + התראת מייל

פרומפט מוכן לקלוד קוד. הרץ מתוך תיקיית הפרויקט `trip-daily-planner`.
אפשר להגיד פשוט: "קרא את FEATURE-hotel-watch-prompt.md ותתחיל מסבב 0".

---

## מה בונים

הרחבה לטאב "מלונות" הקיים (`HOTELS_BY_LOCATION` / `renderHotels()` ב-`app.js`):

על כל כרטיס מלון יתווסף כפתור **"עקוב 👁️"**. לחיצה פותחת טופס inline קטן (לא `prompt()` חוסם) לבחירת תאריך צ'ק-אין וצ'ק-אאוט (`<input type="date">`). אם למיקום הזה יש ימים תואמים ב-`state.data.days`, הצע ברירת מחדל מתוכם — אבל המשתמש חייב לוכל לערוך לפני אישור.

אחרי אישור, המלון מסומן כנעקב: תג **"עוקב 👁️"** על הכרטיס, לצד סטטוס הבדיקה האחרונה (פנוי / לא פנוי / שגיאה בבדיקה) וזמן הבדיקה האחרונה, בעברית ובסטייל של שאר הכרטיסים. כפתור נוסף לביטול המעקב.

**4 פעמים ביום (כל 6 שעות)** תרוץ בדיקה אוטומטית בשרת: לכל מלון-ותאריכים שנעקב, לבדוק אם יש חדר פנוי, מול שלושה מקורות — **Booking.com, Agoda, ו-Google Hotels** (חיפושים חינמיים בלבד, בלי API בתשלום).

כשמלון עובר ממצב "לא פנוי" ל"פנוי" — **מייל התראה פעם אחת בלבד על כל מעבר כזה** (לא בכל בדיקה כל עוד הוא נשאר פנוי), דרך **Resend**, לשתי הכתובות:
- gil.sofer@gmail.com
- ornitleib27@gmail.com

תוכן המייל: שם המלון, המיקום, תאריכי הצ'ק-אין/אאוט, באיזה מקור/מקורות נמצאה זמינות, וקישור ישיר להזמנה (ה-`bookingUrl` מ-`HOTELS_BY_LOCATION` אם קיים, אחרת קישור חיפוש).

---

## הקשר שמשנה סדרי עדיפויות

**הטיול מתחיל בעוד כשבועיים-שלושה** (ראה `tripSubtitle` — ספטמבר–אוקטובר 2026, והתאריכים בימים עצמם). הפיצ'ר הזה נועד לתפוס ביטולים ברגע האחרון, ויש לו חיי מדף קצרים. **עדיף פתרון פשוט שעובד מהר על פני פתרון אלגנטי.** אל תשקיע בהפשטות, בהכללות "לעתיד", או בתשתית שלא נחוצה לשלושת המקורות האלה.

---

## החלטות שכבר סגורות — אל תפתח מחדש

1. שלושת המקורות: Booking.com, Agoda, Google Hotels. חינמי בלבד, בלי API בתשלום ובלי הרשמה לספק נתונים.
2. מייל דרך **Resend**. שתי הכתובות למעלה.
3. התראה **פעם אחת בלבד** בכל מעבר "לא פנוי" → "פנוי".
4. תג סטטוס + זמן בדיקה אחרונה **מוצגים על כרטיס המלון** באתר.
5. אחסון רשימת המעקב והסטטוסים: **Netlify Blobs**.
6. תדירות: כל 6 שעות (`0 */6 * * *` ב-UTC = 03:00 / 09:00 / 15:00 / 21:00 שעון ישראל).

---

## ההחלטה הפתוחה — תכריע אותה בסבב 0, לא לפני

**איך בכלל בודקים זמינות, ומי מריץ את הבדיקה.** שתי השאלות כרוכות זו בזו:

- אם `fetch` רגיל + פענוח HTML (למשל `cheerio`) מספיק לשלושת האתרים → **Netlify Scheduled Function** מספיקה, והכל נשאר במקום אחד.
- אם צריך דפדפן headless (Playwright/Puppeteer) כי האתרים מרונדרים ב-JS או חוסמים → פונקציית serverless היא כנראה המקום הלא נכון (timeout קצר, מגבלת גודל חבילה, Chromium כבד). החלופה: **GitHub Actions** כמתזמן — הריפו כבר על GitHub, `schedule: cron` כל 6 שעות, ראנר אובונטו מלא, Playwright רץ בלי מגבלות. Netlify Blobs נשאר אותו מאגר משותף (`@netlify/blobs` עובד גם מחוץ לנטליפיי עם `siteID` + token מפורשים).

**אל תנעל את הבחירה לבד — הצג לי ממצאים והמלצה ותקבל אישור.** (זה בדיוק המקרה של "flag tradeoffs and ask before big/risky changes" ב-CLAUDE.md.)

הערות שכדאי לשקלל: ל-GitHub Actions יש cron ב-UTC, הרצות מתוזמנות עלולות להתעכב בעומס, ו-GitHub משבית workflow מתוזמן בריפו ללא פעילות 60 יום (לא בעיה בטווח הרלוונטי). ל-Netlify היתרון של ריכוז הכל במקום אחד ואפס תשתית נוספת.

---

## אילוצים ארכיטקטוניים — קרא לפני שכותב שורת קוד

**1. אין בסיס נתונים.** `state.data` נשמר רק ב-`localStorage` בדפדפן (CLAUDE.md, "Data model & persistence"). הבדיקה המתוזמנת רצה בשרת ו**אין לה גישה ל-localStorage**. לכן רשימת המעקב חייבת לחיות ב-Netlify Blobs, ולא ב-`state.data`. אל תנסה לסנכרן בין השניים — Blobs הוא מקור האמת היחיד לפיצ'ר הזה, והדפדפן קורא ממנו דרך פונקציה.

**2. הריפו הזה גאה בכך שאין בו package manager.** CLAUDE.md אומר במפורש "no build, no package manager, no dev server". הפיצ'ר הזה שובר את זה — תלויות כמו `@netlify/blobs`, `resend`, ואולי `cheerio`/`playwright` מחייבות `package.json`. זה שינוי ארכיטקטוני אמיתי: **הזכר אותו לי בסבב 0**, והקפד שהוא נשאר מוגבל לתלויות של הפונקציות/הסקריפט בלבד — הפרונטאנד (`index.html`/`app.js`/`style.css`) נשאר בדיוק כמו שהוא: ללא build, ללא bundler, טעינה ישירה בדפדפן.

**3. "עדיף לא להציג כלום מאשר להציג משהו שגוי"** — העיקרון המנחה של האתר הזה, וכאן הוא קריטי:
- בדיקה שנחסמה / נכשלה / החזירה משהו לא חד-משמעי = סטטוס **`error`**. לא `unavailable` ולא `available`.
- **לעולם אל תשלח מייל על סמך תוצאה לא ודאית.** false positive כאן גרוע בהרבה מ-false negative — הוא ישלח את המשתמש לרוץ להזמין חדר שלא קיים.
- אם מקור מסוים מתברר כלא ניתן לקריאה אמינה, עדיף לסמן אותו `error` באופן קבוע ולומר לי את זה ביושר, מאשר להמציא היוריסטיקה רופפת.

---

## מבנה נתונים מוצע (Netlify Blobs)

Store למשל `hotel-watches`. מפתח = slug יציב של `hotelName + location + checkin + checkout`.

```json
{
  "hotelName": "...",
  "location": "...",
  "searchLocation": "...",
  "bookingUrl": "... (אם קיים ב-HOTELS_BY_LOCATION)",
  "checkin": "YYYY-MM-DD",
  "checkout": "YYYY-MM-DD",
  "createdAt": "ISO",
  "sources": {
    "booking":      { "status": "available|unavailable|error", "checkedAt": "ISO", "note": "" },
    "agoda":        { "status": "...", "checkedAt": "...", "note": "" },
    "googleHotels": { "status": "...", "checkedAt": "...", "note": "" }
  },
  "lastOverallStatus": "available|unavailable|error",
  "lastAlertSentAt": null
}
```

`lastOverallStatus` הוא מה שמזהה את המעבר: שולחים מייל רק כש-`lastOverallStatus` לא היה `available` ועכשיו לפחות מקור אחד מחזיר `available`.

---

## תוכנית עבודה — סבבים חסומים, commit נפרד לכל סבב

עצור לאישור בסוף כל סבב. אל תריץ את כולם ברצף.

### סבב 0 — בדיקת היתכנות (spike). בלי commit לקוד מוצר.
נסה בפועל לשלוף זמינות למלון אמיתי מהרשימה (למשל אחד מבודווה עם `bookingUrl` קיים) לטווח תאריכים אמיתי, משלושת המקורות. בדוק לכל אחד: האם `fetch` פשוט מחזיר HTML שימושי? האם יש חסימה/CAPTCHA? האם התוכן מרונדר ב-JS? מה הסיגנל שמבדיל "אין חדרים" מ"יש חדרים"?

**מה שאני מצפה לקבל בסוף הסבב:** טבלה קצרה של שלושת המקורות (עובד / לא עובד / דורש דפדפן), המלצה על מתזמן (Netlify מול GitHub Actions) עם נימוק, אזכור של נושא ה-`package.json`, והערכה כמה מהפיצ'ר באמת בר-ביצוע. אם מתברר שרק מקור אחד או שניים עובדים — תגיד את זה במפורש, אל תמתח את התמונה.

### סבב 1 — אחסון + API
`package.json` עם התלויות המינימליות, ו-`netlify/functions/hotel-watch.js`: `GET` (כל רשומות המעקב), `POST` (יצירת מעקב), `DELETE`/`action=remove` (מחיקה). מול Netlify Blobs.

### סבב 2 — פרונטאנד
כפתור "עקוב 👁️" / "בטל מעקב", טופס התאריכים, תג הסטטוס וזמן בדיקה אחרונה על הכרטיס. שים לב: `renderPlaceGroups()` הוא helper גנרי משותף עם wineries ו-restaurants שלא צריכים את הפיצ'ר — כנראה עדיף מסלול ייעודי ל-hotels מאשר לנפח את המשותף. שמור על RTL ועל סטייל זהה לשאר האתר, כולל dark mode.

### סבב 3 — הבודק עצמו
לפי ההכרעה מסבב 0. מודול נפרד לכל מקור, כך שאם אחד נשבר אפשר לתקן אותו לבד בלי לגעת בשניים האחרים. טיפול בשגיאות לפי כלל ה-`error` למעלה.

### סבב 4 — Resend + לוגיקת ההתראה
שליחת המייל, לוגיקת המעבר החד-פעמי, עדכון `lastOverallStatus`/`lastAlertSentAt`. מפתח ב-`RESEND_API_KEY` — **לעולם לא בקוד**, בדיוק כמו `GOOGLE_PLACES_API_KEY` היום. נמענים ב-`ALERT_RECIPIENT_EMAILS` (מופרד בפסיקים) עם fallback בקוד לשתי הכתובות. אפשר לשלוח מ-`onboarding@resend.dev` בלי אימות דומיין.

### סבב 5 — תיעוד וסגירה
סעיף חדש ב-CLAUDE.md בסגנון הסעיף הקיים "Nearby places": איפה חי הדאטה ולמה, מה המתזמן ולמה נבחר, אילו משתני סביבה נדרשים ואיפה, מגבלות הסקרייפינג הידועות, ולוגיקת ההתראה החד-פעמית. עדכן גם את המשפט "no build, no package manager" שכבר לא מדויק. בסוף — רשימת פעולות ידניות שנשארו לי (הרשמה ל-Resend, הגדרת משתני סביבה, commit + push כדי שהאתר החי יתעדכן).

---

## תזכורות מ-CLAUDE.md שחלות כאן

- הזכר לי לעשות commit ו-push בסוף — האתר החי מתעדכן רק אחרי push.
- סמן טרייד-אופים ובקש אישור לפני שינויים גדולים/מסוכנים.
- אין בדיקות ואין linter — אמת שינויים בפועל בדפדפן.
- העברית והכיווניות (RTL) הן בכוונה, לא באג.
